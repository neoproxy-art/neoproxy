import AdminDashboard from '@/components/npos/admin/AdminDashboard';

export const metadata = {
  title: 'NPOS // ADMIN_DASHBOARD',
  description: 'NeoProxy OS Administrative Interface',
};

export default function AdminPage() {
  return <AdminDashboard />;
}
